#!/usr/bin/python3
import random
number = random.randint(-10, 10)
if (number > 0)
{
	printf("%d is positive\number", number);
}
else if (number == 0)
{
	printf("%d is zero\number", number);
}
else
{
	printf("%d is numberegative\number", number);
}

return (0);
